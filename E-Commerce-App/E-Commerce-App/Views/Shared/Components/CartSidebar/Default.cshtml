@model List<Product>

<div class="sidebar text-center">
    <h2>Cart</h2>
    <a href="/Products/GetCart" class="btn btn-primary my-2">View Cart</a>
    <ul>
        @foreach (var product in Model)
        {
            <li>@product.Name - @product.Price</li>
        }
    </ul>
</div>
